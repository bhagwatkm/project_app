
<%= form_with(model: [@organization, project]) do |form| %>
  <% if project.errors.any? %>
    <div class="mb-6 p-4 rounded bg-red-100 text-red-800 border border-red-200">
      <h2 class="font-semibold mb-2"><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>
      <ul class="list-disc list-inside">
        <% project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :name, class: "block mb-1 font-medium text-gray-700" %>
    <%= form.text_field :name, class: "w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>

  <div class="mb-4">
    <%= form.label :description, class: "block mb-1 font-medium text-gray-700" %>
    <%= form.text_area :description, class: "w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>

  <div class="mb-4">
    <%= form.label :status, class: "block mb-1 font-medium text-gray-700" %>
    <%= form.select :status, Project.statuses.keys.map { |s| [s.titleize, s] }, {}%>
    
  </div>

  <div class="mb-4">
    <%= form.label :tag_ids, "Tags", class: "block mb-1 font-medium text-gray-700" %>
    <%= form.select :tag_ids, 
        options_from_collection_for_select(@available_tags || [], :id, :name, project.tag_ids),
        {},
        { 
          multiple: true, 
          class: "w-full",

        } %>
    <p class="mt-1 text-sm text-gray-500">
      Select tags for this task. 
      <%= link_to "Manage tags", tags_path, class: "text-blue-600 hover:underline", target: "_blank" %>
    </p>
  </div>
    <%= form.submit "Save", class: "px-6 py-2 bg-green-600 text-white font-semibold rounded shadow hover:bg-green-700 transition" %>
  </div>
<% end %>
